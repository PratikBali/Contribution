# Day 3 Tasks

## Modern Angular + Dependencies

### Morning Session (4 hours):
1. **9:00-10:00**: Update Angular 15 → 16
   - TypeScript 5.0 updates
   - Angular signals preparation
   - New control flow syntax
   - **Project Scan & Analysis**:
     - Scan complete project for compatibility issues
     - Identify deprecation warnings
     - Check ESLint and TypeScript issues
     - Document migration requirements
   - **Build & Test**:
     - Run `npm run tsc` (TypeScript compilation)
     - Run `npm run build` (Production build)
     - Fix any compilation/build errors

2. **10:00-11:00**: Update Angular 16 → 17
   - TypeScript 5.2 updates
   - New application builder
   - Latest Angular Material
   - **Project Scan & Analysis**:
     - Scan complete project for compatibility issues
     - Identify deprecation warnings
     - Check ESLint and TypeScript issues
     - Document migration requirements
   - **Build & Test**:
     - Run `npm run tsc` (TypeScript compilation)
     - Run `npm run build` (Production build)
     - Fix any compilation/build errors

3. **11:00-12:00**: Update Angular 17 → 18
   - TypeScript 5.5 updates
   - Latest Angular features
   - Final version testing
   - **Project Scan & Analysis**:
     - Scan complete project for compatibility issues
     - Identify deprecation warnings
     - Check ESLint and TypeScript issues
     - Document migration requirements
   - **Build & Test**:
     - Run `npm run tsc` (TypeScript compilation)
     - Run `npm run build` (Production build)
     - Fix any compilation/build errors

### Afternoon Session (4 hours):
4. **1:00-3:00**: Firebase v7 → v10 Migration
   - Complete rewrite of AuthService
   - Modular SDK implementation
   - Test authentication flows
   - **Project Scan & Analysis**:
     - Scan complete project for Firebase compatibility issues
     - Identify deprecated Firebase methods
     - Check authentication service integration
     - Document migration requirements
   - **Build & Test**:
     - Run `npm run tsc` (TypeScript compilation)
     - Run `npm run build` (Production build)
     - Fix any compilation/build errors

5. **3:00-4:00**: Dependencies Update
   - Bootstrap 4 → 5
   - FontAwesome updates
   - RxJS v7+ updates
   - **Project Scan & Analysis**:
     - Scan complete project for dependency compatibility issues
     - Identify deprecated methods and APIs
     - Check CSS/styling compatibility
     - Document migration requirements
   - **Build & Test**:
     - Run `npm run tsc` (TypeScript compilation)
     - Run `npm run build` (Production build)
     - Fix any compilation/build errors

### Deliverables:
- Angular 18 with TypeScript 5.5
- Firebase v10 modular SDK
- Updated modern dependencies
- All builds passing (`npm run tsc` && `npm run build`)
- Zero compilation errors
- Documented issues and fixes
